{
  "name": "@uxcue/extension",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "UXCue MV3 Chrome extension (service worker, content script, side panel).",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "package": "echo 'package:extension is wired in UXL-EXT-001 (Vite + CRXJS build + zip)' && exit 0"
  },
  "dependencies": {
    "@uxcue/schema": "workspace:*",
    "@uxcue/ui": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
