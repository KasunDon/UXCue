# @uxcue/console

**DEFERRED (D016).** Cloud console SPA (CloudFront + S3). Not built until cloud
alpha (Phase 5) is revisited. Plan: docs/05, docs/06 (console screens), stories
`UXL-CONSOLE-001..003`.
