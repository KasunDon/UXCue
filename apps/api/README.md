# @uxcue/api

**DEFERRED (D016).** TypeScript Lambda API (API Gateway HTTP API, DynamoDB, S3,
Cognito). Not built until cloud alpha (Phase 5) is revisited. Plan: docs/05,
stories `UXL-CLOUD-001..004`, `UXL-AUTH-001..002`.
