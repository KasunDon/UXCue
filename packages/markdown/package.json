{
  "name": "@uxcue/markdown",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Pure generators for review.md, per-issue markdown, and the GitHub issue body.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@uxcue/schema": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
